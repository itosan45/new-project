# CLAUDE.md

このリポジトリは「秘書」を唯一の窓口とした、個人用の仮想組織です。
作業を始める前に、必ずこのファイルを読んでください。

## 基本ルール

1. **秘書が唯一の窓口**
   ユーザーとのやり取りは常に `secretary/` を通す。部署の作業が必要な場合は、
   秘書が該当する `departments/<部署名>/` に直接書き込む。部署が増えても、
   ユーザーが話しかける相手は変わらない。

2. **言われなくても記録する**
   意思決定・学び・アイデアは、指示がなくても自動的にファイルへ記録する
   （`secretary/logs/` またはユーザーの依頼内容に応じて `departments/<部署名>/` へ）。

3. **追記のみ、上書き禁止**
   既存ファイルの内容は上書きしない。常に追記で運用する。

4. **同日ファイルは追記で統合**
   同じ日付のログファイルがすでに存在する場合（例: `secretary/logs/2026-08-03.md`）、
   新規作成せず、そのファイルに追記する。

5. **迷ったら inbox へ**
   判断に迷う内容、どの部署にも当てはまらない内容は `secretary/inbox/` に入れる。

## 部署を追加する

新しい部署が必要になったら:

1. `departments/<部署名>/` を作成する
2. `departments/<部署名>/README.md` に役割を1行で書く
3. 下記の「部署一覧」に追記する（既存の行は上書きしない）

## 部署一覧

- marketing: (役割を書く)
- research: (役割を書く)
- finance: (役割を書く)

## 関連ドキュメント

- 運用テンプレート（土台構築・フィードバック反映用プロンプト）: `docs/prompts.md`
