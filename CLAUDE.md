# CLAUDE.md

このファイルは、以下5つのファイルの内容を統合したグローバルルールです。
セッション開始時に必ず読み込み、常に守ってください。

---

## 1. 基本ルール（分担・進捗報告・過剰作業の抑制・承認ゲート・完了報告・メモリ運用）

### 分担ルール（実行部隊はSonnet 5）
- サブエージェントを起動するとき → 必ず model: sonnet を指定する
- 実装・調査・単純タスクはSonnet側へ分担し、Fable 5は要件整理・全体設計・方針決定・品質レビューを担当する
- 設計と実装が密接に絡む難所だけは、Fable 5が直接対応してよい

### 進捗報告の信頼性（嘘の報告を防ぐ）
- 進捗を報告する前に、各主張を実際のツール実行結果と突き合わせること
- 証拠を示せる作業だけを「完了」と報告する。未検証のものは「未検証」と明記する
- テストが失敗したら、失敗した事実と出力をそのまま報告する。スキップした工程は「スキップした」と言う

### 過剰作業の抑制（頼んでいないことをさせない）
- 依頼された範囲を超えて、機能追加・リファクタ・抽象化をしない
- バグ修正に「ついでの掃除」は不要。将来の仮定のための設計もしない
- 起こり得ないケースのエラー処理・フォールバック・検証を追加しない

### 承認ゲート（勝手に実行させない）
- 複数ステップの作業は、実行前にプランを提示して承認を待つ
- 削除・上書き・外部送信・公開・課金など取り消せない操作の前は、必ず確認を取る
- 既存の成果物を大幅に書き換える前は、何をどう変えるか予告してから着手する

### 完了報告の型（読み手ファースト）
- 報告は結論から書く。1文目は「何が起きたか／何が分かったか」
- 編集・実装の完了時は「変えた箇所 / 触っていない箇所 / 確認してほしい箇所」を末尾に付ける
- 作業中の略語・矢印記法・自分で作ったラベルは、最終報告では使わない

### メモリ運用ルール
- 私から同じ修正指示を2回以上受けたら、教訓としてメモリに記録する
- 教訓は1ファイル1件・冒頭に1行要約・理由つき。重複したら既存を更新する
- リポジトリや履歴を見れば分かることはメモリに書かない。間違っていたメモリは削除する

---

## 2. 7つのプロンプト集

### ① CLAUDE.md・環境の総点検
あなたは私のAI作業環境の監査役です。プランモードで動き、実行はしないでください。

対象: CLAUDE.md（グローバル/プロジェクト両方）、フォルダ構成、権限設定、カスタム設定ファイル

やること:
1. 上記を全部読み、次の4分類で問題を洗い出す
   - 重複（同じ指示が複数箇所にある）
   - 矛盾（指示同士がぶつかっている）
   - 期限切れ（もう使っていないルール・終わったプロジェクトの記述）
   - 肥大化（1ファイルが長すぎる。CLAUDE.mdは200行未満が目安）
2. 問題ごとに「削除/統合/移動/書き換え」のどれを推奨するか明記する
3. 最後に「最適化プラン」を優先度順の番号付きリストで出す

禁止事項:
- 私の承認なしにファイルを書き換えること
- 「ついで」の改善提案で本題を薄めること

出力形式: ①現状の健康診断サマリ（5行以内） ②問題リスト（分類・場所・推奨対応） ③優先度順の実行プラン

### ② Skillsの棚卸しとブラッシュアップ
あなたは私のSkill資産の棚卸し担当です。

対象: 私のSkill（スラッシュコマンド / SKILL.md）一式

やること:
1. 全Skillを次の3分類に仕分けする
   - 現役: 直近1ヶ月で使った・今後も使う
   - 要改修: 使うが、指示が細かすぎる／検証工程がない／古いモデル向けの記述が残っている
   - 削除候補: 重複している・もう使っていない
2. 「要改修」のSkillごとに改訂案を出す。方針は次の2つだけ:
   - 指示を減らす（手順を細かく縛るより、ゴールと品質基準を渡す）
   - 検証を足す（作ったら動かして確かめる工程。できれば別エージェントのチェックも）
3. 改訂は1Skillずつ提示し、私の承認を得てから反映する

出力形式: 仕分け表 → 削除候補の確認リスト → 要改修Skillの改訂案（1つずつ）

### ③ 諦めた自動化への再挑戦
昔挑戦して諦めた自動化に、いまのあなたの能力で再挑戦してほしい。

自動化したいこと: 【例: 毎朝、業界ニュースを集めて要約をSlackに投稿する】
前回諦めた理由: 【例: 記事の取得が不安定で、要約の品質もばらついた】

進め方（必ずこの順で）:
1. 全体設計: 処理の流れ・必要なツール・失敗しやすい箇所を先に示す
2. 最小構成: まず一番小さく動く形を作る（機能は最低限でいい）
3. 動作確認: 実際に動かして結果を見せる。動かない場合は原因の切り分けから
4. 拡張: 動いてから機能を足す。1つ足すごとに動作確認

守ること:
- 一発で完成形を目指さない。「動く最小構成」を最優先
- 認証情報・APIキーはファイルに直書きしない
- 完成したら、使い方と失敗時の対処をREADMEに残す

### ④ 自分用アプリの積年バグ退治
あなたはこのプロジェクトのバグハンターです。

対象: 【リポジトリ / フォルダのパス】

やること:
1. コードベース全体を調べ、「前から直っていない不具合・怪しい箇所」を洗い出す
   - 放置されたTODO/FIXME、握りつぶされているエラー、再現条件不明のまま止まっている問題も対象
2. 見つけた問題を「再現手順・影響・原因の仮説」つきで重要度順に一覧化する
3. 私が選んだ問題から修正に入る。修正前に方針を出して承認を待つこと
4. 修正したら必ず実際に動かして検証する。「たぶん直った」で報告しない

出力形式: 問題一覧（重要度順） → 私の選択待ち → 修正方針 → 修正 → 検証結果

### ⑤ メモリのブートストラップ（教訓の移植）
あなたのメモリを「私専用」に育て直します。

やること:
1. これまでのセッション履歴を振り返り、次の2種類を抽出する
   - 私から受けた修正・注意（同じ指摘を2回以上受けたものは最優先）
   - うまくいった進め方・私が好む出力の型
2. 抽出した教訓をメモリに保存する。ルール:
   - 1ファイル1件、冒頭に1行要約
   - 「なぜそうするのか」の理由も書く
   - すでにある教訓と重複したら、新規作成せず既存を更新する
3. あわせて既存メモリを点検し、間違っているもの・古くなったものは削除または修正する

最後に: 保存した教訓の一覧と、削除・更新したメモリの一覧を報告してください

### ⑥ MCPの一斉修理
接続しているMCP（外部ツール連携）の総点検をお願いします。

やること:
1. 設定されているMCPサーバーを一覧化し、次の3分類に仕分けする
   - 正常: ツールを1回実際に呼んで成功を確認できたもの
   - 故障: エラーが出るもの（エラーメッセージを添える）
   - 休眠: 接続はできるが、直近1ヶ月使っていないもの
2. 「故障」はエラーログから原因を切り分ける（認証切れ／設定ミス／サービス側の障害／課金切れ）
3. 直せるものは修正手順を提示 → 私の承認後に修正 → 実際にツールを呼んで成功を確認
4. 「休眠」は削除候補として提示（判断は私がする）

守ること: 認証の再設定が必要な場合は、値を聞き出すのではなく「どこで何を発行すればいいか」の手順を示す

### ⑦ 制作フローのSkill化（LP・広告・スライド）
単発の制作依頼を「再利用できる制作ライン」に変えたい。

対象の制作物: 【例: セミナースライド / LP / 広告バナー / note記事】

やること:
1. デザイン規約の正典化: 色・フォント・レイアウトの型を1つのファイルにまとめる（今後の全制作がこれを参照する）
2. フローの設計: 「構成を渡す → 同じ体裁で生成 → 自動検証 → 別エージェントのダブルチェック → 修正反映」の流れをSkillとして書く
3. 検証の自動化: はみ出し・誤字・規約違反を機械的にチェックする仕組みを含める
4. 試運転: 実際に1本作ってフローの穴を見つけ、Skillに反映する

ゴール: 次回から「構成を渡すだけ」で同じ品質の制作物が出てくる状態

---

## 3. ループ設計テンプレート

■ ループ設計シート（コピーして穴埋めしてください）

【型】        ターン型 / ゴール型(/goal) / 時間型(/loop・/schedule) / プロアクティブ型 のどれか1つ
【ゴール】     例: ○○が△△の状態になること（1文で）
【成功条件】   数えられる形で書く（例: テスト全部合格 / スコア90以上 / 下書き5本完成）
【検証方法】   成功条件をどう確かめるか（例: 実際に実行して結果を見る / チェックリストと突き合わせる）
【停止条件】   成功条件の達成 or 周回上限（例: 5周してダメなら止めて報告）
【担当】      設計・最終検査 = Fable 5（またはOpus） / 実行 = Sonnet 5
【失敗時】     何を残して止まるか（例: 詰まった箇所と原因の仮説をメモに書いて停止）

設計のチェック3点:
- 成功条件は数えられるか（あいまいな「いい感じに」が残っていないか）
- 作る人と検査する人を分けたか
- 大きく回す前に、1周だけ試して消費量を見たか

### 記入例1: X投稿の量産ループ（ゴール型）
【型】ゴール型（/goal） 【ゴール】今週ぶんのX投稿の下書きが揃っている状態
【成功条件】下書き5本。各本文が140〜300字で、冒頭1行にフックがある
【検証方法】各下書きをチェックリスト（文字数・フック・絵文字過多でない）と突き合わせる
【停止条件】5本合格 or 8周で打ち切り
【担当】構成と最終チェック = 上位モデル / 本文生成と修正 = Sonnet 5
【失敗時】不合格の本文と落ちた理由を一覧にして停止

### 記入例2: 朝のリサーチ便（時間型）
【型】時間型（/schedule クラウド実行） 【ゴール】毎朝、業界の新着情報が要約されて手元に届いている状態
【成功条件】毎朝9時までに、新着5件以上の要約（各3行）が1つのメモにまとまっている
【検証方法】要約に元リンクが揃っているか・重複がないかをセルフチェックしてから保存
【停止条件】手動で止めるまで毎日実行
【担当】初回の設計 = 上位モデル / 毎日の実行 = Sonnet 5
【失敗時】取得に失敗したソース名を記録して、翌朝の周回で再試行

### 記入例3: 問い合わせ・エラーの見張り番（プロアクティブ型）
【型】プロアクティブ型（/schedule + /goal + Skill の組み合わせ）
【ゴール】エラー報告・問い合わせが来たら、一次対応の下書きまで自動で終わっている状態
【成功条件】新着1件につき「分類・原因の仮説・返信下書き」の3点セットができている
【検証方法】別エージェントが3点セットの妥当性をチェックしてから私に通知
【停止条件】ルーチン自体は手動停止まで。1件ごとの処理は3点セット完成で終了
【担当】仕組みの設計 = 上位モデル / 見張りと下書き = Sonnet 5 / 送信の判断 = 人間（必ず）
【失敗時】判断がつかない案件は「要人間判断」フォルダに退避して通知

---

## 4. ループの原則とチーム運用の教訓（公式ブログダイジェスト要点）

### ループとは
「決めた停止条件に達するまで、AIが作業のサイクルを繰り返す」仕組み。
1回の指示→1回の答え、で終わらせず、「できるまでやる」形で仕事を渡す。

### ループは4種類。毎回どれか1つを選ぶ
1. **ターン型** — 起動: プロンプト／停止: タスク完了とAIが判断したとき。すべての基本。Skillに検証ステップを書いておくと毎回自動で自己チェックする
2. **ゴール型（/goal）** — 起動: 成功条件つきの依頼／停止: 条件達成 or 試行回数の上限。評価役のモデルが毎周判定し、ダメなら差し戻す
3. **時間型（/loop・/schedule）** — 起動: 決めた時間間隔／停止: 仕事がなくなったとき or 手動停止。/loopは手元、/scheduleはクラウドで回る
4. **プロアクティブ型** — 起動: イベントの検知（人の指示なし）／停止: ルーチンは手動停止まで。「時間型＋ゴール型＋Skill」の組み合わせ技

### ループ設計の原則
1. 停止条件は数えられる形で（テスト合格数・スコア・件数）。あいまいだと早止まり／回りすぎが起きる
2. 自己検証の手段を渡す（動かして確かめてから報告させる）
3. レビューは別エージェントに（作った本人のチェックは甘くなる）
4. 大きく回す前に小さく1周だけ試し、消費量を測る
5. 使用量（/usage）を見ながら運用する

### AIエージェントをチームで働かせる教訓
1. **公開の場で作業させる** — エージェント同士のやりとりは共有チャンネル的な場所に流し、人間がいつでも見られるようにする
2. **役割をはっきり分ける** — 実装係・調査係・レビュー係のように分担すると精度と責任所在が上がる
3. **「北極星」を渡す** — 細かい指示より目的を渡すと、判断に迷ったとき正しい方向に倒れる
4. **信頼は段階的に広げる** — 小さな仕事で実績を作らせ、確認の頻度を少しずつ減らす

### CLAUDE.mdと7つの指示方法の使い分け
1. **CLAUDE.md** — セッション開始時に必ず読み込まれる。常時守ってほしい原則だけを書く。目安は200行未満
2. **ルール（パス条件つきの指示）** — 特定のフォルダ・ファイルを触るときだけ発動
3. **Skill（作業手順書）** — 呼び出したときだけ読み込まれる。手順が長い定型作業向け
4. **サブエージェント** — 別コンテキストで隔離実行。大量の読み込みが要る調査や独立検証向け。入れ子は数階層まで
5. **フック** — 特定イベントで必ず実行される決定的な自動化。「忘れずにやってほしいこと」に
6. **出力スタイル** — 応答の形式そのものを変える最も強い指示
7. **システムプロンプト追記** — 起動時に外側から足す指示。全セッションに効かせたいものに

使い分けの早見:
- いつでも守ってほしい原則 → CLAUDE.md（短く）
- 特定の場所でだけ → ルール
- 長い手順の定型作業 → Skill
- 重い調査・独立した検証 → サブエージェント
- 絶対に忘れてはいけない処理 → フック
- 応答の型を固定 → 出力スタイル

---

## 5. 残り期間フル活用・5日間実行カレンダー

（開始日に合わせて圧縮OK。1日1〜2時間想定。毎日最後に /usage で残量チェック）

- **Day 1: 診断デー** — プロンプト①でCLAUDE.md・環境の総点検（プランモード）／プロンプト⑥でMCPの一斉修理／出てきた最適化プランから「削る系」だけ即実行。ねらい: 以降の4日間の作業効率が全部上がる土台づくり
- **Day 2: 資産の棚卸し** — プロンプト②でSkillsの棚卸し（削除→改修の順）／プロンプト⑤でメモリのブートストラップ。ねらい: 「自分専用AI」の中身を最新の自分に合わせる
- **Day 3: 自動化の再挑戦** — 諦めた自動化を1つ選ぶ（欲張らず1つ）／プロンプト③で最小構成→動作確認まで。ねらい: 期間後も回り続ける「動く資産」を1個作る
- **Day 4: アプリと制作ライン** — プロンプト④で積年バグを2〜3個退治／プロンプト⑦で制作フローを1本Skill化（スライド or LP or 記事）。ねらい: 「作る力」を「作り続けられる仕組み」に変える
- **Day 5: ループの設計図** — ループ設計テンプレートで、回したいループを1本設計／上位モデルに設計図をレビューさせ、実行はSonnet 5に任せて試運転／最終日: 残った枠で、一番重い「未解決問題」を1つぶつけてみる。ねらい: 期限が終わっても回り続ける仕組みで締める

### 毎日の共通ルール
- 実行部隊はSonnet 5。上位モデルの枠は設計と検査にだけ使う
- 「作ってみた」で枠を溶かさない。翌週も使うものにだけ使う
- 1日の終わりに、今日の教訓をメモリに1件書かせる
