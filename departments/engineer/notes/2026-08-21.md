# 2026-08-21

- 12:10 `secretary/inbox/2026-08-21T11-59-53-000Z.md` の「AIアプリ 自動起動＆
  時間になったら話しかけてくるシステム」設計図について、開発可否の意見を
  求められたため技術評価を実施。
  - Windows/macOS デスクトップ常駐版は枯れた技術の組み合わせ
    （タスクスケジューラ/LaunchAgent + APScheduler + OpenAI API + TTS +
    システムトレイ）で、数日でMVP可能と判断。
  - iOS/Android の「自動起動」は設計書どおりには作れない。iOSはサードパーティ
    アプリの起動時自動起動・無確認バックグラウンド実行APIが無く、Androidも
    Doze/バッテリー最適化・端末メーカー独自制限で定刻発火が不安定になる。
  - ⑦マルチデバイス同期は認証・サーバー・push通知を要する別プロジェクト規模。
  - 結論として本人に提示：まずWindows/macOSのデスクトップMVPから着手し、
    スマホ側は「自動起動」ではなく「時刻指定ローカル通知＋タップで会話開始」
    に設計を落とし込むことを推奨。着手可否は本人の判断待ち。

- 13:00 本人がMVP案を却下しフル機能路線を選択（詳細は
  `decisions/2026-08-21-ai-companion-full-scope.md`）。Androidから着手、
  パーソナライズ情報はGoogleドライブに置く方針、途中でGoogle Home連携も
  追加要望。以下を実装。
  - `companion-android/`: Android実機アプリの骨組み一式
    - `AlarmScheduler` / `AlarmReceiver`: `AlarmManager.setExactAndAllowWhileIdle`
      による正確な時刻起動（目覚まし時計アプリと同じ方式）
    - `BootReceiver`: 端末再起動後にアラームを再登録
    - `SpeakService`: `AlarmReceiver`からフォアグラウンドサービスとして起動し、
      `TextToSpeech`でメッセージを読み上げ後、翌日分を再スケジュールして終了
    - `MainActivity`: 時刻・メッセージ設定UIと「今すぐ試しに話させる」ボタン。
      Android 13+の通知権限、Android 12+の正確なアラーム権限を実行時に要求
    - メッセージ本文はまだ仮のローカル入力欄。Googleドライブ連携は未実装
      （次の一手）
  - `.github/workflows/android-build.yml`: pushのたびにdebug APKを自動ビルドし
    Actionsのartifactとして出力する仕組み。この環境にAndroid実機/SDKが無く
    実行確認ができないため、実際のビルド確認はCIに委ねる方針
  - 音声デモとして、この環境で急遽 gTTS（Google翻訳のTTS）を使い試作音声を
    本人に送付。「ひどい音声」との評価。実機のAndroid標準TTSエンジンや
    Google Home自体の音声とは別物であることを説明済み
  - Google Home連携（在宅時のトリガーをGoogle Homeから発話させる）は
    Google Cast（Chromecast）プロトコルでローカルWi-Fi内から音声URLを
    再生指示する方式が現実的と判断。実装は未着手（次の一手）

- 21:20 本人から「アプリ名わかりやすく、アイコンも」との要望。
  - アプリ名を仮の「AI話しかけ」から「よびかけAI」に変更(`strings.xml`)
  - アイコンを単色の鈴マークから、吹き出し＋3点リーダー(会話中を示す
    デザイン)のAdaptive Icon(前景/背景レイヤー分離)に作り直し。
    `minSdk=26`がAdaptive Icon対応の最小バージョンと一致するため、
    旧式の解像度別ビットマップは用意せず`mipmap-anydpi-v26`のみで対応
  - マニフェストの`android:icon`/`roundIcon`を`@mipmap/ic_launcher`に変更

- 21:40 Gemini APIキーを本人から受領(テキストファイル添付)。形式が
  通常の`AIzaSy...`と異なり不審だったため一度確認したところ、本人が
  「間違いない」と明言。実際にこの環境からGemini APIへ直接叩いて検証し、
  有効なキーであることを確認(ただし`gemini-2.0-flash`は廃止済みで
  `gemini-3.6-flash`への切り替えが必要だった)。
  - `webapp/app/api/companion/route.ts` を新規追加。Androidアプリには
    キーを埋め込まず、既存のVercelデプロイ(`webapp/`)をプロキシにする
    構成。`x-companion-secret`ヘッダーによる簡易認証を追加
    (`lib/auth.ts`の`timingSafeEqual`をexportして再利用)
  - lint・build・既存テスト94件がすべて通過することを確認
  - **未設定**: Vercel側の環境変数`GEMINI_API_KEY`・`COMPANION_SHARED_SECRET`
    はこちらでは設定できない(Vercelの管理画面操作権限が無いため)。
    本人による設定待ち
  - キー自体はどのファイル・コミットにも書き込んでいない

- 21:50 AIの名前を「ヨビ」に決定。Googleドライブに以下を作成
  (`references/`ではなく実データなので直接ドライブに作成):
  - `ヨビ/habits.md` (夕飯/運動/勉強などの目標・ノルマを本人が書く場所)
  - `ヨビ/logs/` (会話履歴を日付ごとに貯める場所、今後実装)

- 22:00 「ユーザーの呼び名は、この場(開発時のチャット)で決めるのではなく、
  アプリの初回起動時にオンボーディング画面で入力させるべき」との指摘。
  対象ユーザーが変わりうるため、開発時に固定するのは筋が悪いとの理由。
  尤もな指摘のため以下を実装:
  - `OnboardingActivity` を新設し、これをランチャーにした
    (`MainActivity`は非公開化)。初回起動時に呼び名を入力させ、
    `SharedPreferences`(`AlarmScheduler.KEY_USER_NAME`)に保存後
    `MainActivity`へ遷移。2回目以降は自動で`MainActivity`へスキップ
  - `MainActivity`に呼び名の表示と「呼び名を変更する」リンクを追加
    (押すと呼び名を消してオンボーディングへ戻る)
  - Googleドライブの「ユーザー名でフォルダを作る」処理自体はまだ未実装
    (現時点ではローカルに呼び名を保存するところまで。フォルダ作成は
    サーバー側でGoogle Drive APIの認証情報を別途用意する必要があり、
    次の一手)
