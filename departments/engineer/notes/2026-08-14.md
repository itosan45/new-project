# 2026-08-14

- 09:00 画像・PDF → Excel 自動入力ツールを構築(`ocr-excel/`)。
  Vision API ではなく認証済みの Google Drive の読み取り機能を採用し、
  利用者側のセットアップをゼロにした。テスト13件すべて成功。
- 10:00 友人(ChatGPT / Codex 利用者)向けの配布版を作成
  (`ocr-excel/友人配布用/`)。1ファイル完結の `転記.py` と、
  そのまま貼れる ChatGPT用・Codex用プロンプトを同梱。
  ChatGPT版はインストール不要で毎日使える形にした。
- 14:00 ハーネスの4画面と骨組み(lib/domain/types.ts)を実装(`harness/`)。
  型・データ・UIを分離し、画面はドメイン型だけを読む構成にした。
- 15:30 仮想顧客3社(住宅リフォーム/税理士法人/化粧品D2C)と
  ペルソナ9名を設計し `/tenants` として実装。
  3社とも承認ゲートを必要とするが、守っているものが
  「品質」「責任」「爆発半径」と異なることを設計の軸に据えた。
- 17:00 Agentの専門性を設計。契約に expertise / notSuitableFor /
  qualityRisks / escalatesWhen を追加し、「できること」と同じ重さで
  「使ってはいけない場面」を持たせた。
  業種別の知識は「共通Agent × ドメインパック」で持たせる構成にし、
  業種ごとにAgentを作り分ける案は却下(業種×Agentの数だけ実装が要るため)。
