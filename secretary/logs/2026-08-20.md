# 2026-08-20

## Googleドライブに成果物を置こうとした

依頼: 「Googleドライブで claude フォルダ作って、その中にフォルダ作って入れて」

やったこと:

- ドライブに `claude` フォルダを作成
  https://drive.google.com/drive/folders/1pQip89EyCIJOg3WaBhizL9EJH7D7zNay
- その中に `遠州急行_モック_2026-08-19` フォルダを作成
- `はじめに.md`（説明書き）を置いた

できなかったこと:

- **画像とPDFをドライブに入れること。** 中身が壊れる。
  詳しくは `decisions/2026-08-20-成果物の受け渡し経路.md`

代わりにやったこと:

- デザイン案のPDFとPC画像を、チャットに直接添付して渡した
- スマホ画像は縦が長すぎて添付できなかったので、3枚に分けて渡した

## 学んだこと

1. ドライブに送れるのは、実質テキストだけ。画像は自分でアップロードしてもらう。
2. 縦に長い画像は、重さではなく縦の長さで弾かれる。5000ピクセルを目安に分ける。
3. 「入れておきました」と言う前に、入ったファイルのサイズを元と照合する。
   照合しなければ、壊れたものを渡したことに気づけなかった。

## /adminのKPIを実データに置き換えた

「/adminの数字を本物にする」を選んでもらったので実施。

- 上4枚のKPI（稼働中エージェント・本日の処理・承認待ち・削減効果）を、
  固定値から `cases/` 配下の実際の案件と `lib/agents/registry.ts` から
  数える形に変えた
- 円換算（¥1,860,000のような数字）はやめて、時間のまま出すことにした。
  時間単価の根拠が無いため
- 前日比の矢印・スパークラインも削除した。比較対象の「昨日の値」を
  どこにも保存していないので、本物の数字を入れる先が無かった
- 詳しくは `decisions/2026-08-20-admin-kpi-real.md`

まだサンプルデータのまま: ワークフロー健康状態、承認待ちキューの中身、
自動化効果の推移のグラフ、エージェント活動状況の一覧（こちらは共通の
運用エージェント群の見た目で、この案件ストアとは別物）。

## 案件受付に書類を添付できるようにした

「案件受付に書類を添付できるようにする」を選んでもらったので実施。

- `/cases` の受付フォームに書類欄を追加。全文を貼り付けるか、
  `.txt`/`.md`/`.csv` ファイルを選んで読み込める
- 画像・PDFは選べないようにした（OCRがまだ無いのに選べると、
  読めたと誤解されるため）
- `lib/engine/pipeline.ts` に `documents` の受け口を追加。渡せば
  Document Readerが実際に動き、渡さなければ今まで通り入力待ちで止まる
- テストで両方の経路（渡す／渡さない）を確認済み
- 詳しくは `decisions/2026-08-20-case-intake-documents.md`

まだやっていないこと：画像・PDFからの本物のOCR接続
（`ocr-excel/`を実際に呼ぶところ）。今回は「渡す口」を作っただけ。

## Classifierを実装し、ECパックを実際に動かした

「専門度をレベルアップしたい」という依頼を受けて選択肢を出したところ、
「ECパックの穴を埋める」を選んでもらった。

調べたところ、EC・D2Cパック（ルミエール向け）は実装のあるAgentが
1体も差し込まれておらず、抽出項目が一度も動いたことが無かった。

- classifierを実装（問い合わせ本文を区分に振り分ける）
- 「感情の強さ 0〜1」のような根拠のない実数は作らず、
  「区分」という新しい抽出の種類（強い／通常など）を追加した
- Document Readerと項目の探し方を共有する形にした
- テストで、配送・品質・強い苦情などの実例が正しく区分されることを確認
- 詳しくは `decisions/2026-08-20-classifier-ec-pack.md`

まだやっていないこと：EC・D2Cパックの残り3体
（draft-writer, voice-of-customer, data-analyst）は契約だけのまま。

## 「ペルソナ案件を自動生成する」というアイデアへの返答

セッション中に「他のエージェントにペルソナ案件をどんどん作ってもらって、
コントロールセンターでこなして精度を上げる」という案が出た。方向性は
良いが、`/admin`のKPIをつい先ほど「cases/配下の実件数」で数える形に
直したばかりなので、生成した架空の案件を同じ場所に積むと数字が
また作り話に戻ってしまう、という注意点を伝えた。実装はまだしていない。
